#!/usr/bin/env node
const fs=require("node:fs"),path=require("node:path"),campaign=require("../data/phoenix-super-enrichment-campaign.json"),places=require("../data/generated/launch-map-places.json"),root=path.resolve(__dirname,".."),fail=[];
for(const e of campaign.places){const p=places.find(x=>x.id===e.id);if(!p){fail.push(`${e.name}: missing`);continue;}if(!p.image?.url)fail.push(`${p.name}: hero missing`);if(1+(p.images?.length||0)<4)fail.push(`${p.name}: fewer than 4 photos`);if((p.features||[]).length!==8)fail.push(`${p.name}: expected exactly 8 subsites`);if((p.searchAnswers||[]).length<11)fail.push(`${p.name}: answers missing`);for(const f of p.features||[]){if(!f.details?.imageUrl)fail.push(`${p.name}/${f.name}: image missing`);if((f.details?.searchAnswers||[]).length<9)fail.push(`${p.name}/${f.name}: fewer than 9 answers`);if(!Number.isFinite(f.latitude)||!Number.isFinite(f.longitude))fail.push(`${p.name}/${f.name}: coordinate missing`);if(!f.details?.coordinateSource||!f.details?.positionQuality)fail.push(`${p.name}/${f.name}: coordinate provenance missing`);const featureFile=path.join(root,"us/az/phoenix/parks",p.slug,f.slug,"index.html");if(!fs.existsSync(featureFile)){fail.push(`${p.name}/${f.name}: page missing`);continue;}const featureHtml=fs.readFileSync(featureFile,"utf8");for(const v of[f.name,p.name,"parking","restroom","dogs","Sources","rel=\"canonical\""])if(v&&!featureHtml.toLowerCase().includes(v.toLowerCase()))fail.push(`${p.name}/${f.name}: raw HTML missing ${v}`);}const file=path.join(root,"us/az/phoenix/parks",p.slug,"index.html");if(!fs.existsSync(file)){fail.push(`${p.name}: page missing`);continue;}const html=fs.readFileSync(file,"utf8");for(const v of[p.name,p.address,"rel=\"canonical\"","What people ask"])if(v&&!html.includes(v))fail.push(`${p.name}: raw HTML missing ${v}`);}
const text=id=>JSON.stringify(places.find(p=>p.id===id)||{}).toLowerCase();for(const [id,label,phrases] of[
  ["launch-az-phoenix-papago-park","Papago",["east and west parking","ramadas require reservations","hole-in-the-rock"]],
  ["launch-az-phoenix-south-mountain-park-and-preserve","South Mountain",["silent sunday","extreme heat warnings","dobbins lookout"]],
  ["launch-az-phoenix-camelback-mountain","Camelback",["august 10-21","dogs are prohibited","extremely difficult"]],
  ["launch-az-phoenix-rio-salado-habitat-restoration-area","Rio Salado",["fishing is strictly prohibited","dogs must stay on paved trails","audubon center"]]
])for(const phrase of phrases)if(!text(id).includes(phrase))fail.push(`${label}: ${phrase} missing`);if(fail.length){console.error(fail.map(x=>`- ${x}`).join("\n"));process.exit(1);}console.log(`Verified ${campaign.places.length} Phoenix guides with sourced galleries, mapped subsites, and practical visitor answers.`);
