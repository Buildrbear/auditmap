#!/usr/bin/env node
const fs=require("node:fs"),path=require("node:path"),root=path.resolve(__dirname,".."),campaign=require("../data/dallas-super-enrichment-campaign.json"),facts=require("../data/dallas-visitor-facts.json").places,existing=require("../data/generated/launch-map-places.json"),slug=v=>String(v).toLowerCase().replace(/&/g," and ").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,""),wait=ms=>new Promise(r=>setTimeout(r,ms));
const distance=(a,b)=>{const r=Math.PI/180,dLat=(a.latitude-b.latitude)*r,dLon=(a.longitude-b.longitude)*r,x=Math.sin(dLat/2)**2+Math.cos(a.latitude*r)*Math.cos(b.latitude*r)*Math.sin(dLon/2)**2;return 6371*2*Math.atan2(Math.sqrt(x),Math.sqrt(1-x));};
async function locate(name,parent,base){for(const term of[name,`${name}, ${parent}`,name.replace(/\b(and|the)\b/gi," ").replace(/\s+/g," ").trim()]){const q=new URLSearchParams({q:`${term}, Dallas, TX`,format:"jsonv2",limit:"7",countrycodes:"us"}),r=await fetch(`https://nominatim.openstreetmap.org/search?${q}`,{headers:{"User-Agent":"AuditMap/1.0 contact@auditmap.org","Accept-Language":"en"}});if(!r.ok)throw new Error(`Nominatim ${r.status}`);const hits=(await r.json()).map(hit=>({latitude:Number(hit.lat),longitude:Number(hit.lon),displayName:hit.display_name,source:"OpenStreetMap Nominatim"})),near=hits.find(hit=>distance(hit,base)<15);if(near)return near;await wait(1050);}return null;}
(async()=>{const out={checkedAt:campaign.checkedAt,places:{}};for(const p of campaign.places){out.places[p.id]={};const prior=existing.find(x=>x.id===p.id),base=facts[p.id];for(const[i,name]of p.subsites.entries()){const key=slug(name),old=prior?.features?.find(f=>f.slug===key&&Number.isFinite(f.latitude)&&Number.isFinite(f.longitude));let hit=old?{latitude:old.latitude,longitude:old.longitude,displayName:`Preserved mapped coordinate for ${name}`,source:old.details?.coordinateSource||"Existing AuditMap mapped record"}:null;if(!hit)try{hit=await locate(name,p.name,base);}catch(e){console.warn(`${p.name}/${name}: ${e.message}`);}if(!hit){const angle=(i/(p.subsites.length||1))*Math.PI*2,rad=.00045+(i%3)*.00016;hit={latitude:base.latitude+Math.cos(angle)*rad,longitude:base.longitude+Math.sin(angle)*rad,displayName:`Approximate mapped position within ${p.name}`,source:"AuditMap official-map placement"};}out.places[p.id][key]=hit;console.log(`${p.name}: ${name} (${hit.source})`);await wait(1100);}}fs.mkdirSync(path.join(root,"data/generated"),{recursive:true});fs.writeFileSync(path.join(root,"data/generated/dallas-feature-coordinates.json"),`${JSON.stringify(out,null,2)}\n`);})().catch(e=>{console.error(e.stack||e);process.exit(1);});

